# MRF Hyperparameters
# Per-band reconstruction parameters.
#
# NOTE: k_scale and offset were removed as redundant: k_scale was never
# consumed (MrfRec.initializeBand is not used by the pipeline) and the
# per-band offset was superseded by the shared BSFI offset search.

crystallographic_data: [5.8077, 5.8077, 9.1297, 90, 90, 120]

bands:
  - index: 0
    eta: 0.0000065
  - index: 1
    eta: 0.0000000065
  - index: 2
    eta: 0.05
  - index: 3
    eta: 0.000000065
  - index: 4
    eta: 0.0045
